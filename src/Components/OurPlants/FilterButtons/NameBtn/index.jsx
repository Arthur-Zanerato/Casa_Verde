import { Button } from "../styles";

const NameFilter = () => {
  const OrderName

  return (
    <>
      <Button>
        <button onClick={OrderName}>Nome</button>
      </Button>
    </>
  );
};

export default NameFilter;
